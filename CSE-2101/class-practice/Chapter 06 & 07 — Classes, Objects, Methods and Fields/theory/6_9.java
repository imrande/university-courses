/*
What does a constructor do? What are the syntactic differences between a
constructor and a method?
*/

A constructor is initializes immediately when the object is created..

The syntactic differences between a constructor and a method is constructor has no return
type and a method must have return type, void if it doesn' t return anything
