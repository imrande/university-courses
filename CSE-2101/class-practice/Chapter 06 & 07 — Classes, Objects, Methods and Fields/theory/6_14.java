// What is access specifier? Distinguish among them with examples.

An access specifier is a keyword that tells how the class member can be accessed.

4 types of access specifier ==>
(1) private ==> declared as private specifier can be accessed only from its class
(2) public ==> declared as public specifier can be accessed from anywhere
(3) default access ==> declared as default access can be accessed from anywhere
in the same package.
(4) protected ==> decalared as protected specifier can be accessed from anywhere in the same package
and from its subclass
