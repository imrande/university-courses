/*
The simplist type of data structure is linear array. it's one dimensonal array.
by a linear array we mean a list of finite numbers n of similliar data elements

Example ==>
if we choose name of array is A, then the elements of arrays are
denoted by subscript notation
1) parenthesis notation
A(1), A(2), A(3), A(N)
2) Bracket notation
A[1], A[2], A[3], A[N]
3) a1, a2, a3, an

A is called subscript
A[N] is called subscripted varaiable
*/
