// Write the feature of destructor

destructor is a special member fn which destroy the object once its requirement is complete.
It is done to release the memory space occupied by the obj so that release memory can be
used in other some purpose...

Feature :-
1) Destructor also has the class name. To make a distinction between destructors and
constructors, tilde(~) symbol is used with the class name in case of destructors
2) It does not have any return type
3) Can't be overloaded
4) Destructor is executed implicitly

