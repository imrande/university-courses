/*
In C++ program only 2 types error
1) Syntax error [compile time error]
2) Logical error [runtime error]
*/

// Syntax of error handling

try{
  --------;
  ---------;
  --------;

  throw(exception type); // should be same with catch(exception)
}
 catch(exception type){ // should be same with throw(exception)
   ----------;
   -------;
 }
